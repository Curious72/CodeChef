#include <iostream>
#include<algorithm>
#include<cstdio>
using namespace std;
 
int main()
{
 
int h;
scanf("%d",&h);
int i=0;
int A[h];
while(i<h)
{
 
    scanf("%d",&A[i]);
    i++;
 
}
    sort(A,A+h);
    for(i=0;i<h;i++)
    {
        printf("%d \n",A[i]);
    }
return 0;
}
 
